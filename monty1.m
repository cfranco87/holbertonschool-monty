#include "header.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>


void push(int value);

int main(void)
{
	int n;
	
	if (n != 0)
	{
		printf("push integer");
		exit(STDERR, "EXIT_FAILURE\n");
	}

	strcpy(&str);

	val = atoi(str);
	printf(str);

	return (0); 
}
